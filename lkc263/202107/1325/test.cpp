#include<iostream>
#include<cstdio>
#include<vector>
#include<algorithm>
#define MAX 10001

using namespace std;

bool visit[MAX]; //방문했는지 체크

vector <int> v[MAX];

int n, m;

int cnt = 0;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	return 0;
}
